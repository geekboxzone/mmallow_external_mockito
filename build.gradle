apply plugin: 'java'

sourceSets {
    main {
        java {
            srcDirs = ['src']
        }
    }
}

jar {
    baseName 'mockito'
    appendix 'api'
}

dependencies {
    compile getAndroidPrebuilt('10')
    compile project(':external:objenesis')
    compile project(':external:junit')
}
